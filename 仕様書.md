# 気分の天気図 — 仕様書 v3

東京23区版 / Cloudflare Workers + Hono + React / 制限時間5時間

---

## コンセプト

天気アプリのように、**街の「気分」を投稿し、地図で見る**アプリ。

天気は空を見れば分かるが、街の気分は誰にも見えない。
アプリ起動時に今の気分を投げてもらい、直近6時間の平均を区ごとに天気として描く。

---

## 課題設定（発表の冒頭で言うこと）

- 都市で暮らす人の日々の気分の浮き沈みは、SNSに書くには重く、日記アプリでは自分の中で完結してしまい、誰にも共有されない
- 「今しんどいのは自分だけか」を確かめる手段がなく、孤立感を強めてしまう
- 街全体の「今の気分」を**天気のように可視化する**ことで、自分の気分を軽い気持ちで投げ込みつつ、同じ街の他の人の気分も一望できるようにするのが本プロダクト

---

# MUST

これだけで発表は成立する。ここが終わるまで OPTION に手を出さない。

## M1. 起動時の投稿フロー

アプリを開いたとき、**前回投稿から6時間以上経っていれば**入力画面を表示する。
6時間以内なら、そのまま地図画面へ。

判定は `localStorage` に保存した最終投稿時刻で行う（サーバー側では管理しない）。

初回起動時に**端末ID（UUID）を生成してlocalStorageに保存**する。ログイン機能は作らない。投稿・マイページの取得は、すべてこの端末IDに紐付ける（別端末からは見えない）。

## M2. 位置情報の取得

ブラウザの **Geolocation API** で現在地の緯度経度を取得する。
取得した座標から**23区のどれかを判定**する。ユーザーに区を選ばせる欄は作らない。

### 区の判定方法（重要）

正確なポリゴン判定は時間がかかるため、**各区の代表座標との最近傍**で判定する。

- 23区それぞれの代表座標（区役所の位置など）を定数として持つ
- 取得した座標から最も近い区を選ぶ
- 最寄りの区まで **15km以上離れていたら「区外」** とし、投稿できない旨を表示する

### 座標の扱い（発表で言うこと）

- 座標は**区の判定にのみ使い、DBには保存しない**
- 保存するのは区名だけ

### 位置情報を拒否された場合

許可ダイアログで拒否されることは必ずある。**フォールバックを用意する。**

- 「位置情報が使えません。エリアを選んでください」と表示
- 23区のセレクトボックスを出して手動選択させる

## M3. 気分の入力

| 項目 | 必須 | 仕様 |
|---|---|---|
| 気分度 | 必須 | 1〜5の5段階。大きめのボタン。スマホ前提 |
| コメント | 任意 | 100文字まで。プレースホルダ「ひとことどうぞ」 |

区は M2 で自動判定されるので、画面には「渋谷区にいます」と表示するだけ。

## M4. AIによるおすすめ行動のサジェスト

投稿直後に、その場でおすすめ行動や参考情報を一言表示する。

- 入力は**本人が今回入力したscore/comment** と **投稿した区の直近6時間の平均・天気**のみに限定する
  - 他ユーザーの個別コメントは使わない（3件未満非表示という匿名性ルールと矛盾させないため）
- 出力は短い一言アドバイス（例:「街も少し曇り気味です。10分だけ外に出てみませんか」）
- Workers AI で生成する
- **生成に失敗した場合は固定文リストからランダム表示するフォールバックを必須とする**（M7のアラート文言と同じ方針）

## M5. マイページ

端末IDに紐づく自分の投稿履歴を、区の推移と並べて表示する。

- 表示項目：日時・score・commentの時系列一覧（自分の投稿のみ）
- 比較表示：同じ期間の「投稿した区」の平均スコア推移を隣に並べる
- 目的は「自分だけ下がっている日」が一目でわかること（課題設定の裏付けになる画面）
- ログイン不要。**端末をまたぐと閲覧できない**旨をUIに明記する

## M6. 地図表示

- 23区を**格子状のタイル**で配置（地図ライブラリは使わない）
- 各タイルは**直近6時間**の平均気分度で色が変わる

| 平均 | 天気 | 色 |
|---|---|---|
| 1.0〜2.0 | 雨 | 青 |
| 2.0〜2.8 | くもり時々雨 | 水色 |
| 2.8〜3.5 | くもり | グレー |
| 3.5〜5.0 | 晴れ | オレンジ |

- **投稿が3件未満の区は色を付けず**「データ不足」と表示する
- 上部に東京全体の平均を大きく表示
- **30秒ごとに自動更新**

### タイル配置

実際の地理に近い格子で並べる。

```
行1:              板橋   北    足立
行2:        練馬  豊島   荒川  葛飾
行3:  中野  新宿  文京   台東  墨田  江戸川
行4:  杉並  渋谷  千代田 中央  江東
行5:  世田谷 目黒 港
行6:  大田  品川
```

## M7. アラート

直近6時間の区平均が閾値を超えたら、その区にアラートを表示する。

| 種別 | 条件 | 文言例 |
|---|---|---|
| 低 | 平均 **2.5未満** | 「渋谷区、雨模様です。何かありましたか？」 |
| 高 | 平均 **3.5超** | 「世田谷区、よく晴れています。何かいいことが？」 |

- **投稿3件未満の区ではアラートを出さない**（M6の非表示ルールと揃える）
- 閾値は**1箇所に定数としてまとめる**（当日データを見て調整するため）
- 他区との比較・優劣で語らない。医療的な判断表現は使わない
- 文言は Workers AI で生成してもよいが、**失敗時は固定文にフォールバック必須**

## M8. デプロイ

Cloudflare Workers 上で動作していること。**開始30分以内に空の状態で一度デプロイする。**

---

# OPTION

MUST完了後、優先順に着手する。

| 優先 | 機能 | 内容 |
|---|---|---|
| 1 | **属性の収集** | 初回のみ性別・年齢層を入力。`localStorage` に保存し投稿時に送信。**画面には一切表示しない**（将来の分析用にDBへ蓄積するのみ） |
| 2 | AIによる天気の一文生成 | コメント群から「渋谷区は今日、薄曇り」のような一文を生成 |
| 3 | 区タップでコメント一覧 | その区の直近コメントを表示 |
| 4 | 時間帯スライダー | 6時間前→現在の推移をアニメーション |
| 5 | アラートの原因推測 | AIがイベント・天候等から理由を推測 |
| 6 | 曜日・時間帯別の傾向グラフ | データ蓄積後 |
| 7 | 平常値との差分アラート | 運用データが溜まってから。今回は閾値方式 |

### 属性収集の設計（OPTION 1 を実装する場合）

- ログイン・アカウント登録は作らない。初回に1画面出すだけ
- 性別：男性 / 女性 / その他 / **回答しない**（必須で入れる）
- 年齢：**年齢層**で選択（10代 / 20代 / 30代 / 40代 / 50代 / 60代以上）。生年月日は取らない
- **スキップボタンを必ず置く**（入力欄が増えるとデモで投げてもらえなくなる）
- 集計API・地図には属性を一切反映しない

---

## データ設計

### テーブル

属性カラムは**最初から用意しておく**（後から ALTER TABLE するより早い）。
MUST 段階では常に NULL で構わない。

```sql
CREATE TABLE IF NOT EXISTS moods (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  device_id  TEXT    NOT NULL,
  ward       TEXT    NOT NULL,
  score      INTEGER NOT NULL,
  comment    TEXT,
  gender     TEXT,
  age_group  TEXT,
  created_at TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_moods_created ON moods(created_at);
CREATE INDEX IF NOT EXISTS idx_moods_device  ON moods(device_id, created_at);
```

`device_id` は M1 で発行する端末UUID。マイページ（M5）の履歴表示に使う。ログインは作らないため、この値だけが本人と投稿を結びつける唯一のキー。

緯度経度のカラムは作らない。

### API

**POST /api/moods**

```json
{
  "deviceId": "5f2b3c1a-...",
  "ward": "shibuya",
  "score": 4,
  "comment": "いい感じ",
  "gender": null,
  "ageGroup": null
}
```

- `deviceId` は必須（M1でlocalStorageに保存したUUID）
- `ward` は定義済みの23区IDのみ許可
- `score` は 1〜5 の整数のみ
- `comment` は100文字で切り詰め
- 不正値は 400
- 返り値にAIサジェスト（M4）を含める

```json
{ "ok": true, "suggestion": "街も少し曇り気味です。10分だけ外に出てみませんか" }
```

- `suggestion` の生成に失敗した場合は、固定文リストからランダムに1つ返す（AI呼び出し失敗を理由に投稿自体を失敗させない）

**GET /api/mypage?deviceId=xxx**

指定した端末IDの投稿履歴と、投稿した区の同期間平均を並べて返す（M5）。

```json
{
  "history": [
    { "date": "2026-08-08", "score": 2, "comment": "ちょっと疲れた", "ward": "shibuya" },
    { "date": "2026-08-07", "score": 4, "comment": null, "ward": "shibuya" }
  ],
  "wardAverage": [
    { "date": "2026-08-08", "average": 3.1 },
    { "date": "2026-08-07", "average": 3.4 }
  ]
}
```

- `deviceId` に紐づく投稿がなければ `history: []`
- 集計は投稿日単位（同日に複数投稿があれば平均）

**GET /api/summary**

直近6時間を集計。

```json
{
  "wards": [
    { "ward": "shibuya", "name": "渋谷区", "count": 12,
      "average": 2.1, "enough": true, "weather": "rain" },
    { "ward": "ota", "name": "大田区", "count": 2,
      "average": null, "enough": false, "weather": null }
  ],
  "total": 87,
  "overall": 3.2,
  "alerts": [
    { "ward": "shibuya", "type": "low",
      "message": "渋谷区、雨模様です。何かありましたか？" }
  ]
}
```

- 全23区を必ず含める（0件でも `count: 0`）
- `count < 3` なら `average` と `weather` は null、`enough` は false

### 共通定義ファイル

`src/shared/wards.ts` に以下を置き、フロント・バック双方から参照する。

- 23区の id / 表示名 / 代表座標（緯度経度）/ タイル配置の行列位置
- 閾値定数（`ALERT_LOW = 2.5` / `ALERT_HIGH = 3.5` / `MIN_COUNT = 3` / `WINDOW_HOURS = 6`）

---

## シードデータ（必須作業）

参加者だけでは1区あたり数件しか集まらず、地図がほぼ灰色になる。

- **過去7日分 × 23区 × 各日10件程度**（約1,600件）をランダム生成
- スコアはランダムでよい（区ごとの個性は不要）
- `device_id` は `NOT NULL` のため、シード行ごとにランダムなUUIDを割り当てる（マイページの動作確認には影響しないダミー値でよい）
- ただし直近6時間について、**意図的に以下を仕込む**
  - **低アラートが出る区を1つ**（平均2.0程度）
  - **高アラートが出る区を1つ**（平均4.2程度）
  - **データ不足の区を2〜3個**（投稿0〜2件のまま残す）
- `--local` と `--remote` の**両方**に投入する
- 発表では「デモ用のシードデータです」と正直に述べる

---

## デモ準備チェックリスト

- [ ] 本番URLをスマホで開いて動く
- [ ] `--remote` にシードデータが入っている
- [ ] 低アラート・高アラート・データ不足がすべて画面に出ている
- [ ] **6時間制限をデモ用に解除するスイッチ**がある（審査員が何度も投稿できるように）
- [ ] 位置情報を拒否した場合のフォールバックが動く
- [ ] 投稿後にAIサジェストが表示される（フォールバック文言も含めて確認）
- [ ] マイページで「自分の推移」と「区の推移」が並んで表示される
- [ ] QRコードを用意した
- [ ] リハーサルを2回通した

---

## 発表で必ず答えられるようにしておく

| 想定質問 | 回答 |
|---|---|
| それChatGPTでよくない？ | 個人の相談ではなく、街という単位の集合的状態を扱っている |
| 位置情報はどう扱っている？ | 区の判定にのみ使い、座標はDBに保存しない |
| なぜ空白の区がある？ | 3件未満は個人が特定され得るため、あえて隠している |
| 地域差別にならない？ | 他区との比較では表示せず、その区の状態のみを問いかける形で表現している |
| 閾値の根拠は？ | 5段階の中央3から±0.5。運用データが溜まれば平常値との差分方式に移行する |
| なぜCloudflare？ | D1で永続化、Workersで会場からの同時アクセスに対応、Workers AIで外部APIキー不要 |

---

## 未決事項（決めてから実装に入ること）

1. 会場は何区か。参加者の投稿が1区に集中するため、デモの見せ方に影響する
2. アプリ名 / `wrangler.jsonc` の `name`（公開URLになる）
3. 4人の担当割り当て
4. 属性収集（OPTION 1）を実装するかの最終判断
5. MUSTがM1〜M8の8機能に増えたため、5時間で完成できるかの見積もり直し。厳しければAIサジェスト（M4）かマイページ（M5）のどちらかをOPTION格下げする判断を早めにする